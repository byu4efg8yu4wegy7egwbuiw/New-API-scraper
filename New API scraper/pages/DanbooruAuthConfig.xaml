<Page x:Class="New_API_scraper.pages.DanbooruAuthConfig"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:New_API_scraper.pages"
      mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="800"
      Title="DanbooruAuthConfig"
      Background="#FF0A0A0A">

    <Page.Resources>
        <Style x:Key="ModernTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#FF1A1A1A"/>
            <Setter Property="Foreground" Value="#FFEEEEEE"/>
            <Setter Property="BorderBrush" Value="#FF333333"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="CaretBrush" Value="#FFEEEEEE"/>
            <Setter Property="SelectionBrush" Value="#FF0099FF"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Name="border" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                        Focusable="false" 
                                        HorizontalScrollBarVisibility="Hidden" 
                                        VerticalScrollBarVisibility="Hidden"
                                        Padding="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF0066CC"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="true">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF0099FF"/>
                                <Setter TargetName="border" Property="Background" Value="#FF252525"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernPasswordBoxStyle" TargetType="PasswordBox">
            <Setter Property="Background" Value="#FF1A1A1A"/>
            <Setter Property="Foreground" Value="#FFEEEEEE"/>
            <Setter Property="BorderBrush" Value="#FF333333"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="CaretBrush" Value="#FFEEEEEE"/>
            <Setter Property="SelectionBrush" Value="#FF0099FF"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border Name="border" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                        Focusable="false" 
                                        HorizontalScrollBarVisibility="Hidden" 
                                        VerticalScrollBarVisibility="Hidden"
                                        Padding="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF0066CC"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="true">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF0099FF"/>
                                <Setter TargetName="border" Property="Background" Value="#FF252525"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FF0066CC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Name="border" 
                                Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center" 
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="border" Property="Background" Value="#FF0088FF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="true">
                                <Setter TargetName="border" Property="Background" Value="#FF0044AA"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FF333333"/>
            <Setter Property="Foreground" Value="#FFCCCCCC"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Name="border" 
                                Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center" 
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="border" Property="Background" Value="#FF444444"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="true">
                                <Setter TargetName="border" Property="Background" Value="#FF222222"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="40" MinHeight="600">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="30"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="25"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="25"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="25"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="30"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="50"/>
            </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" HorizontalAlignment="Center">
            <TextBlock Text="Danbooru Authentication" 
                       FontSize="28" 
                       FontWeight="Bold" 
                       Foreground="#FF0099FF" 
                       HorizontalAlignment="Center"
                       Margin="0,0,0,10"/>
            <TextBlock Text="Enter your Danbooru login credentials to access the API" 
                       FontSize="14" 
                       Foreground="#FFAAAAAA" 
                       HorizontalAlignment="Center"/>
        </StackPanel>

        <!-- Username Field -->
        <StackPanel Grid.Row="2">
            <TextBlock Text="Username:" 
                       FontSize="16" 
                       FontWeight="Bold" 
                       Foreground="#FFCCCCCC" 
                       Margin="0,0,0,8"/>
            <TextBox x:Name="username_textbox" 
                     Style="{StaticResource ModernTextBoxStyle}"
                     Height="45" TextChanged="username_textbox_TextChanged" Text=""/>
        </StackPanel>

        <!-- API Key Field -->
        <StackPanel Grid.Row="4">
            <TextBlock Text="API Key:" 
                       FontSize="16" 
                       FontWeight="Bold" 
                       Foreground="#FFCCCCCC" 
                       Margin="0,0,0,8"/>
            <PasswordBox x:Name="api_key_passwordbox" 
                         Style="{StaticResource ModernPasswordBoxStyle}"
                         Height="45"/>
        </StackPanel>

        <!-- Instructions -->
        <Border Grid.Row="6" 
                Background="#FF1A1A1A" 
                BorderBrush="#FF333333" 
                BorderThickness="1" 
                CornerRadius="8" 
                Padding="15">
            <StackPanel>
                <TextBlock Text="How to get your API key:" 
                           FontSize="14" 
                           FontWeight="Bold" 
                           Foreground="#FF0099FF" 
                           Margin="0,0,0,8"/>
                <TextBlock Text="1. Go to your Danbooru profile page" 
                           FontSize="12" 
                           Foreground="#FFAAAAAA" 
                           Margin="0,0,0,4"/>
                <TextBlock Text="2. Click the 'Generate API key' button" 
                           FontSize="12" 
                           Foreground="#FFAAAAAA" 
                           Margin="0,0,0,4"/>
                <TextBlock Text="3. Copy the generated API key and paste it above" 
                           FontSize="12" 
                           Foreground="#FFAAAAAA" 
                           Margin="0,0,0,8"/>
                <TextBlock Text="⚠️ Keep your API key secret! It provides full access to your account." 
                           FontSize="12" 
                           FontWeight="Bold" 
                           Foreground="#FFFF6666"/>
            </StackPanel>
        </Border>

        <!-- Status -->
        <TextBlock x:Name="status_text" 
                   Grid.Row="8" 
                   Text="Enter your credentials above" 
                   FontSize="14" 
                   Foreground="#FFAAAAAA" 
                   HorizontalAlignment="Center"
                   Margin="0,15,0,0"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="10" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Center"
                    Margin="0,20,0,20">
            <Button x:Name="test_btn" 
                    Content="Test Connection" 
                    Style="{StaticResource SecondaryButtonStyle}"
                    Click="test_btn_click"
                    Width="130"
                    Height="40"
                    Margin="0,0,10,0"/>
            <Button x:Name="save_btn" 
                    Content="Save &amp; Continue" 
                    Style="{StaticResource ModernButtonStyle}"
                    Click="save_btn_click"
                    Width="130"
                    Height="40"
                    Margin="0,0,10,0"/>
            <Button x:Name="skip_btn" 
                    Content="Skip (Anonymous)" 
                    Style="{StaticResource SecondaryButtonStyle}"
                    Click="skip_btn_click"
                    Width="130"
                    Height="40"
                    Margin="0,0,0,0"/>
        </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>
