<Window x:Class="New_API_scraper.ApiSelectorWin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:New_API_scraper"
        mc:Ignorable="d"
        Title="API Selector" Height="600" Width="1000"
        Background="Transparent"
        WindowStyle="None"
        AllowsTransparency="True"
        WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <Style x:Key="ModernComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="#FF1A1A1A"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#FF333333"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="15">
                            <Border.Effect>
                                <DropShadowEffect Color="#FF00FF88" BlurRadius="8" ShadowDepth="0" Opacity="0.3"/>
                            </Border.Effect>
                            <Grid>
                                <ToggleButton x:Name="ToggleButton"
                                            Background="Transparent"
                                            BorderThickness="0"
                                            IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                            ClickMode="Press">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="ToggleButton">
                                            <Border Background="Transparent"/>
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                </ToggleButton>
                                <ContentPresenter x:Name="ContentSite"
                                                IsHitTestVisible="False"
                                                Content="{TemplateBinding SelectionBoxItem}"
                                                ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                Margin="{TemplateBinding Padding}"
                                                VerticalAlignment="Center"
                                                HorizontalAlignment="Left"/>
                                <Popup x:Name="Popup"
                                     Placement="Bottom"
                                     IsOpen="{TemplateBinding IsDropDownOpen}"
                                     AllowsTransparency="True"
                                     Focusable="False"
                                     PopupAnimation="Slide">
                                    <Border Background="#FF1A1A1A"
                                          BorderBrush="#FF333333"
                                          BorderThickness="1"
                                          CornerRadius="15"
                                          MaxHeight="200">
                                        <Border.Effect>
                                            <DropShadowEffect Color="#FF00FF88" BlurRadius="15" ShadowDepth="0" Opacity="0.4"/>
                                        </Border.Effect>
                                        <ScrollViewer>
                                            <StackPanel IsItemsHost="True"/>
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="ModernComboBoxItemStyle" TargetType="ComboBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="item_border" 
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="8">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="item_border" Property="Background" Value="#FF333333"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="item_border" Property="Background" Value="#FF444444"/>
                            </Trigger>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="item_border" Property="Background" Value="#FF333333"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FF00AA44"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="20"
                                x:Name="border">
                            <Border.Effect>
                                <DropShadowEffect Color="#FF00FF88" BlurRadius="15" ShadowDepth="0" Opacity="0.6"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FF00CC55"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FF008833"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Border CornerRadius="25" Background="#FF0A0A0A" BorderBrush="Transparent" BorderThickness="0">
        <Grid>
            <Canvas x:Name="bg_canvas" ClipToBounds="True" Background="Transparent"/>
            
            <Canvas x:Name="effect_canvas" ClipToBounds="True"/>
            
            <Canvas x:Name="notification_canvas" ClipToBounds="False" IsHitTestVisible="False"/>
            
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="100"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="50"/>
                </Grid.RowDefinitions>
                
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="450"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Row="0" Grid.Column="1"
                          Text="API SELECTOR"
                          FontSize="36"
                          FontWeight="Bold"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center"
                          Margin="0,30,0,0">
                    <TextBlock.Foreground>
                        <LinearGradientBrush x:Name="chroma_brush" StartPoint="0,0" EndPoint="1,0">
                            <GradientStop Color="#FF00FF44" Offset="0"/>
                            <GradientStop Color="#FF44FF88" Offset="0.2"/>
                            <GradientStop Color="#FF88FFAA" Offset="0.4"/>
                            <GradientStop Color="#FFAAFFCC" Offset="0.6"/>
                            <GradientStop Color="#FF66FF99" Offset="0.8"/>
                            <GradientStop Color="#FF22FF66" Offset="1"/>
                        </LinearGradientBrush>
                    </TextBlock.Foreground>
                    <TextBlock.Effect>
                        <DropShadowEffect x:Name="glow_effect" Color="#FF00FF88" BlurRadius="25" ShadowDepth="0" Opacity="0.9"/>
                    </TextBlock.Effect>
                    <TextBlock.RenderTransform>
                        <ScaleTransform x:Name="title_scale" ScaleX="1" ScaleY="1" CenterX="225" CenterY="18"/>
                    </TextBlock.RenderTransform>
                </TextBlock>
                
                <Border Grid.Row="2" Grid.Column="1" 
                        Background="#FF111111" 
                        CornerRadius="20" 
                        BorderBrush="#FF333333" 
                        BorderThickness="2"
                        Margin="30,15">
                    <Border.Effect>
                        <DropShadowEffect Color="#FF00FF88" BlurRadius="10" ShadowDepth="0" Opacity="0.2"/>
                    </Border.Effect>
                    <ComboBox x:Name="api_dropdown" 
                              Height="55" 
                              Margin="25"
                              FontSize="18"
                              Style="{StaticResource ModernComboBoxStyle}">
                        <ComboBox.ItemContainerStyle>
                            <StaticResource ResourceKey="ModernComboBoxItemStyle"/>
                        </ComboBox.ItemContainerStyle>
                        <!-- Items will be loaded dynamically -->
                    </ComboBox>
                </Border>
                
                <Button x:Name="select_btn" 
                        Grid.Row="3" 
                        Grid.Column="1" 
                        Height="55" 
                        Margin="30,15,30,25"
                        Content="INITIALIZE CONNECTION"
                        FontSize="18"
                        Style="{StaticResource ModernButtonStyle}"
                        Click="select_btn_click"/>
                
                <Button Grid.Row="0" Grid.Column="2"
                        Content="×"
                        Width="40"
                        Height="40"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        Margin="0,15,15,0"
                        Background="#FF333333"
                        Foreground="#FFCCCCCC"
                        BorderThickness="0"
                        FontSize="20"
                        FontWeight="Bold"
                        Cursor="Hand"
                        Click="close_btn_click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    CornerRadius="25"
                                    x:Name="close_border">
                                <ContentPresenter HorizontalAlignment="Center"
                                                VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="close_border" Property="Background" Value="#FFAA3333"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </Grid>
    </Border>
</Window>